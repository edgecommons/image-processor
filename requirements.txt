psutil>=5.9.6
awsiotsdk>=1.19.0
awsiot>=0.1.3
paho-mqtt>=2.0.0
watchdog>=3.0.0
# The edgecommons Python library. The CLI substitutes the dependency form you chose with
# `--dep-source`: a pinned git requirement for `registry`, or an editable install of a local
# sibling checkout for `local`. Bump it later with `edgecommons component upgrade --to <ver>`.
edgecommons @ git+https://github.com/edgecommons/edgecommons@8e688894845152de612abe39e04c9e07d3f183c5#subdirectory=libs/python

# WP1 (contracts): JSON Schema 2020-12 validation of config.schema.json and schemas/*.json.
# WP2 (bundles): Ed25519 bundle signatures and model-bundle-manifest schema validation.
# `boto3` stays out of this list: `s3://` model sources come from the `s3` extra
# (`pip install .[s3]`), and without it an `s3://` URI fails with BundleError("S3_UNAVAILABLE").
cryptography>=42
jsonschema>=4.21
# WP4a -- engine: decode, task families, decision rules.
numpy>=1.26
pillow>=10
jsonpath-ng>=1.6
# The CPU ONNX Runtime (the `cpu` extra). A GPU host installs `.[gpu,nvml]` instead of this file:
# `onnxruntime` and `onnxruntime-gpu` ship the same package and must never both be installed.
onnxruntime>=1.17
